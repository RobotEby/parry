# This directory is a reusable Terraform stack module.
# Provider configuration lives in environments/* so callers can pass the
# default regional AWS provider plus the us-east-1 alias required by
# CloudFront-scoped AWS WAF resources.
